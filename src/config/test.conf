# global comment

server {
    listen 8080;
    root /var/www;

    server_name example.com localhost;

    methods GET POST;

    client_max_body_size 5M;

    error_page 404 /errors/404.html;

    location /images {
        root /data/images;
        autoindex on;
        methods GET;
    }
}